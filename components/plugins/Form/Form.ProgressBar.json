{
  "Name": "Form.ProgressBar",
  "Method": "Component",
  "Desc": "Progress bar component with min, max, val methods",
  "LongDesc": "The ProgressBar component provides a visual progress bar for forms, implemented using only div elements. It supports min, max, and value methods for controlling the progress, and can be styled via CSS variables.",
  "Type": "Component",
  "Example": [
    "var pb = Q.Form.ProgressBar(0, 100, 30);",
    "pb.max(200).min(10).val(50);"
  ],
  "Dependencies": [
    "Form",
    "style",
    "find",
    "css"
  ]
}
